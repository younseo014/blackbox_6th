# 메모리 가드 정량 점수 계산 지표 설계안

> 상태: **설계안 — 프로그램 미적용**  
> 작성일: 2026-08-27  
> 목적: 현재 프로토타입에서 이미 수집·계산 가능한 값만으로, 개인 기준선 대비 업무 흐름의 변화를 일관되게 점수화한다.

---

## 1. 용어와 원칙

### 사용자에게 보여줄 이름

- 점수 이름: **평소 흐름 일치도**
- 범위: 0~100점
- 의미: 건강 상태나 진단 점수가 아니라, **오늘의 업무 흐름이 본인의 평소 패턴과 얼마나 비슷했는지**를 보여주는 참고 지표

### 내부 분석용 이름

- 이벤트 변화 근거 점수: `Event Change Evidence (ECE)`
- 일일 변화 부담: `Daily Change Burden (DCB)`
- 일일 평소 흐름 일치도: `Daily Flow Similarity (DFS)`

`위험도`, `인지 저하 점수`, `치매 점수` 같은 표현은 사용하지 않는다.

### 설계 원칙

1. 절대 시간보다 **동일 사용자·동일 업무의 개인 기준선**을 우선한다.
2. 한 번의 사건으로 알리지 않고, **서로 다른 업무 또는 여러 날짜에서 반복되는 변화**를 본다.
3. 현재 신뢰도 있게 측정할 수 없는 대화·손님 거리·정확한 작업 중단은 점수에 억지로 넣지 않는다.
4. 학습 모드에서 변화 후보는 기준선에 포함하지 않는다.
5. 점수는 사용자에게 단정적으로 해석하지 않고, 근거와 함께 제공한다.

---

## 2. 현재 프로토타입에서 사용할 수 있는 입력값

| 변수 | 설명 | 현재 수집 상태 |
|---|---|---|
| `durationSeconds` | 목표 동작 구간의 총 소요 시간 | 사용 가능 |
| `longestPauseSeconds` | 동작 중 가장 긴 정지 시간 | 사용 가능 |
| `durationZScore` | 동일 업무 소요 시간의 개인 기준선 대비 Z-score | 사용 가능 |
| `pauseZScore` | 동일 업무 정지 시간의 개인 기준선 대비 Z-score | 사용 가능 |
| `taskConfidence` | 업무 라벨 추정 신뢰도 | 사용 가능 |
| `contextWeight` | 휴식 0 / 일반 1 / 이동 중 정체 2 / 작업 중 정체 3 | 사용 가능 |
| `taskType`, `date` | 업무 종류·관찰 날짜 | 사용 가능 |
| `routeComplexity`, `repetitionCount` | 경로 복잡도·반복 움직임 | 기록 중이나 개인별 SD 기준선 미구현 |
| 손님과의 거리·마주보기 | 대화 중 지연을 제외하기 위한 정보 | 아직 점수 사용 불가 |
| 작업 중단·복귀 시점 | Task Switching Cost / Resumption Lag | 아직 점수 사용 불가 |

---

## 3. 점수 계산 전: 분석 대상 자격 조건

점수는 아무 카메라 구간에나 계산하지 않는다. 아래 조건을 모두 만족한 구간만 분석 대상이다.

### 3.1. 업무 기준선 자격

| 조건 | 기준 | 처리 |
|---|---:|---|
| 개인 기준선 전체 완성도 | 70% 이상 | 미만이면 점수 대신 “기준선을 익히는 중” 표시 |
| 동일 업무 표본 수 | 8회 이상 | 8~19회는 참고용 점수만 산출, 20회 이상일 때 사용자 알림 적격 |
| 표준편차 | `SD ≥ 0.05` | 너무 작은 SD로 인한 과민 반응 방지 |
| 관찰 모드 | 분석 모드 | 학습 모드의 결과는 기준선 후보로만 처리 |

### 3.2. 동작 구간 자격

| 조건 | 기준 | 처리 |
|---|---:|---|
| 업무 라벨 신뢰도 | `taskConfidence ≥ 0.55` | 미만이면 점수 제외 |
| 휴식 구역/휴식 업무 | `contextWeight = 0` | 점수 제외 |
| Z-score | 소요시간 또는 정지시간 중 하나라도 계산 가능 | 둘 다 `null`이면 점수 제외 |
| 동일 업무 중복 | 동일 업무·2시간 구간 내 여러 사건 | 가장 높은 점수 1건만 반영 |

> `0.55`, `8회`, `2시간`은 현 단계의 시작값이다. 실제 매장 파일럿에서 오탐률을 보고 조정한다. 특히 `SD ≥ 0.05`는 목표 성능이 아니라 0에 가까운 분모를 막는 안전장치다.

---

## 4. 이벤트 변화 근거 점수 (ECE)

### 4.1. Z-score 초과분 정규화

임계값인 `1.5 SD`를 넘은 정도만 점수화한다.

$$
E(z) = \min\left(1,\ \max\left(0, \frac{z - 1.5}{1.5}\right)\right)
$$

| Z-score | `E(z)` | 의미 |
|---:|---:|---|
| 1.5 이하 | 0.00 | 개인 평소 범위 또는 알림 기준 미만 |
| 2.0 | 0.33 | 변화 후보 |
| 2.5 | 0.67 | 뚜렷한 변화 후보 |
| 3.0 이상 | 1.00 | 상한값 적용 |

상한을 두는 이유는 카메라 오류나 비정상적으로 긴 한 구간이 하루 점수를 지배하지 않게 하기 위해서다.

### 4.2. 시간·정지 결합 변화량

$$
M_i = 0.6 \times E(Z_{duration,i}) + 0.4 \times E(Z_{pause,i})
$$

- 소요시간 변화에 60%, 최장 정지 변화에 40%를 둔다.
- 한 값이 계산되지 않으면 계산 가능한 값 하나를 100% 사용한다.
- 둘 다 계산되지 않으면 이벤트 점수를 만들지 않는다.

### 4.3. 상황 가중치

| 맥락 | 현재 판정 조건 | 가중치 `W` |
|---|---|---:|
| 휴식 | `REST` 업무 | 0 |
| 일반 업무 중 변화 | 일반 업무 구간 | 1 |
| 이동 중 정체 | Zone 전환 + 2초 이상 정지 | 2 |
| 작업 중 정체·복귀 지연 후보 | 활동이 있던 업무 중 5초 이상 정지 | 3 |

### 4.4. 이벤트 변화 근거 점수

$$
ECE_i = W_i \times M_i
$$

- 범위: `0.00 ~ 3.00`
- 값이 높을수록 “그 업무 구간이 개인 기준선과 달랐다는 근거”가 강함을 뜻한다.
- 의학적 위험도나 진단 확률이 아니다.

### 4.5. 계산 예시

| 항목 | 값 |
|---|---:|
| 음료 제조 소요시간 Z-score | 2.4 |
| 최장 정지시간 Z-score | 2.1 |
| 맥락 | 작업 중 정체·복귀 지연 후보 (`W = 3`) |

$$
E(2.4) = \frac{2.4 - 1.5}{1.5} = 0.60
$$

$$
E(2.1) = \frac{2.1 - 1.5}{1.5} = 0.40
$$

$$
M = 0.6 \times 0.60 + 0.4 \times 0.40 = 0.52
$$

$$
ECE = 3 \times 0.52 = 1.56
$$

이 구간은 “평소보다 소요시간과 멈춤이 함께 늘어난 작업 중 정체 후보”로 기록할 수 있다.

---

## 5. 일일 변화 부담 (DCB)

하루에 같은 업무가 반복되어도 점수가 무한히 쌓이지 않게 하기 위해 중복을 제한한다.

### 5.1. 중복 제한 규칙

1. 동일 `taskType` + 동일 2시간 구간에서는 `ECE`가 가장 높은 1건만 반영한다.
2. 한 업무가 하루에 반영할 수 있는 최대치는 `3.0`으로 제한한다.
3. 하루 전체 변화 부담은 `9.0`을 상한으로 제한한다.

### 5.2. 계산식

$$
DCB_d = \min\left(9,\ \sum_{i \in d} ECE_i\right)
$$

| DCB | 내부 해석 | 사용자 노출 방식 |
|---:|---|---|
| 0.0 ~ 1.0 | 평소 범위 또는 약한 단발성 변화 | 기본적으로 별도 알림 없음 |
| 1.1 ~ 3.0 | 한두 업무에서 변화 후보 | “조금 더 살펴보기” |
| 3.1 ~ 6.0 | 여러 맥락 또는 뚜렷한 변화 | 케어 기록에 근거 표시 |
| 6.1 ~ 9.0 | 같은 날 여러 독립 업무에서 변화 | 반복 여부를 다음 며칠 관찰 |

---

## 6. 사용자용 평소 흐름 일치도 (DFS)

일일 변화 부담을 사용자가 읽기 쉬운 0~100점으로 바꾼다.

$$
DFS_d = \max\left(40,\ \text{round}\left(100 \times e^{-0.1 \times DCB_d}\right)\right)
$$

| DCB | DFS 예시 | 사용자 문구 |
|---:|---:|---|
| 0.0 | 100점 | 평소와 비슷한 흐름이에요 |
| 1.0 | 90점 | 대체로 평소와 비슷해요 |
| 2.0 | 82점 | 한두 가지 흐름을 살펴봐요 |
| 3.0 | 74점 | 평소와 다른 흐름이 관찰됐어요 |
| 4.0 | 67점 | 여러 업무에서 변화가 있었어요 |
| 6.0 | 55점 | 변화가 반복되는지 확인해 볼게요 |
| 9.0 이상 | 40점 | 오늘은 평소와 다른 업무 흐름이 여러 번 있었어요 |

### 사용자 화면 상태 구간

| DFS | 표시 | 의미 |
|---:|---|---|
| 85~100 | 평소와 비슷 | 변화 근거가 거의 없거나 약함 |
| 70~84 | 조금 더 살펴보기 | 한두 업무에서 변화 후보가 있음 |
| 40~69 | 변화 관찰 | 여러 업무 또는 높은 가중치 맥락에서 변화 후보가 있음 |

> 최저점을 40으로 제한하는 이유는 “0점 = 심각한 상태”처럼 오해될 수 있는 표현을 피하기 위해서다.

---

## 7. 주간 알림 조건

일일 점수 하나만으로는 푸시·케어 알림을 보내지 않는다.

### 7.1. 주간 요약값

$$
WFS = \frac{\sum DFS_d}{N_{eligible\ days}}
$$

- `N_{eligible days}`: 점수 산출 자격을 충족한 관찰일 수
- 관찰일이 3일 미만이면 주간 점수 대신 “기록을 더 모으는 중”으로 표시

### 7.2. 알림 트리거

아래 조건 중 하나를 만족할 때만 케어 기록 상단에 변화를 강조한다.

| 단계 | 조건 | 표시 방식 |
|---|---|---|
| 참고 | 최근 7일 중 `DCB ≥ 3`인 날이 1일 | 기록 안에서만 근거 표시 |
| 살펴보기 | 최근 7일 중 `DCB ≥ 3`인 날이 2일 이상 | “비슷한 변화가 며칠 반복됐어요” |
| 변화 관찰 | 최근 7일 중 `DCB ≥ 4`인 날이 3일 이상 또는 `WFS < 70` | 케어 연결 카드와 함께 표시 |

### 7.3. 알림 억제 조건

다음 중 하나면 점수는 기록하되 사용자 알림은 억제한다.

- 관찰일 3일 미만
- 업무 라벨 신뢰도가 낮은 구간이 대부분인 날
- 사용자가 “바쁜 날”로 표시한 날이 최근 7일의 절반 이상
- 사용자 피드백으로 `대화 중`, `휴식`, `카메라 오류`가 확인된 사건

---

## 8. 현재는 점수에 넣지 않는 항목

| 항목 | 제외 이유 | 도입 조건 |
|---|---|---|
| 손님과의 거리 | 단일 웹캠 2D 좌표는 실제 m 단위 거리로 환산하기 어려움 | 카메라별 보정 또는 3D/다인 추적 |
| 마주보기 판별 | 사장님 외 다른 사람의 안정적 방향 벡터가 없음 | 다인 포즈·방향 추정 신뢰도 확보 |
| 작업 전환 비용 | 중단 업무·새 업무·복귀 시점을 확정할 근거 부족 | POS·예약·도어 센서 또는 안정적 업무 시퀀스 |
| 구역 재방문 Z-score | 평균은 있지만 SD·충분한 개인 표본이 없음 | 업무별 재방문·경로 복잡도 기준선 확장 |
| 반복 동작 횟수 Z-score | 업무 종류에 따른 자연스러운 반복과 분리 필요 | 업무 라벨 정확도와 개인 기준선 확보 |
| EMA 자동 기준선 갱신 | 분석 모드의 변화가 새 평소로 흡수될 위험 | 검증된 정상 구간만 별도 승인하는 정책 |

---

## 9. 기준선 업데이트 정책 (후속 적용)

분석 모드의 모든 데이터를 EMA로 갱신하면 실제 변화 후보까지 기준선에 섞일 수 있다. 따라서 아래 조건에서만 갱신한다.

### 갱신 후보 조건

- 사용자 피드백이 “정확한 분석”이 아니라 **평소 업무였다**고 확인된 구간
- `ECE < 0.5`
- 업무 라벨 신뢰도 `≥ 0.70`
- 바쁜 날·카메라 오류·대화/휴식 구간이 아님

### 느린 갱신식

$$
\mu_t = 0.05x_t + 0.95\mu_{t-1}
$$

EMA는 매 사건이 아니라 주 1회 검토·반영하는 방식으로 시작한다. 표준편차 역시 별도의 온라인 분산 알고리즘을 도입하기 전까지는 최근 검증된 정상 표본으로 재계산한다.

---

## 10. 정확도 검증 지표

현재 단계에서는 “인지 저하를 맞혔는가”가 아니라 **업무 변화 알림이 납득 가능한가**를 평가한다.

| 지표 | 정의 | 초기 목표 |
|---|---|---:|
| 업무 변화 알림 정밀도 | 사용자/관찰자가 “분석이 맞다”고 답한 비율 | 80% 이상 |
| 오탐률 | 대화·휴식·카메라 오류 등으로 잘못 감지된 비율 | 20% 이하 |
| 라벨 신뢰도 | 자동 업무 라벨과 사람이 확인한 업무 라벨의 일치율 | 직군별로 별도 측정 |
| 근거 이해도 | 사용자가 “왜 기록됐는지 이해했다”고 답한 비율 | 80% 이상 |
| 사용자 피로도 | 알림이 과하다고 답한 비율 | 20% 이하 |

> 위 목표값은 제품 검증용 시작 기준이다. 임상 정확도나 의료 성능 수치가 아니다.

---

## 11. 표본 수와 표준편차 적격성 지표

### 11.1. `8회`와 `SD ≥ 0.05`는 다른 조건이다

- `8회`는 동일 업무의 평균·표준편차를 **처음 계산할 수 있는 최소 표본 수**다.
- `SD ≥ 0.05`는 SD가 0에 가까워 Z-score가 과도하게 커지는 것을 막는 **분모 안전장치**다.
- 따라서 “8회면 SD 0.05를 달성한다”는 관계는 성립하지 않는다. SD는 업무의 실제 변동성에 따라 커지거나 작아진다.

정규 분포를 가정할 때 표본 표준편차의 상대 표준오차는 대략 다음과 같다.

$$
RSE(s) \approx \sqrt{\frac{1}{2(n-1)}}
$$

| 동일 업무 표본 수 `n` | SD 추정의 상대 표준오차(근사) | 점수 사용 정책 |
|---:|---:|---|
| 8회 | 26.7% | 탐색·개발용 참고값만 산출 |
| 12회 | 21.3% | 사용자 화면에는 참고 수준으로만 표시 |
| 20회 | 16.2% | 사용자 케어 기록·주간 알림 계산 적격 |
| 30회 | 13.1% | 안정 기준선 후보 |

### 11.2. 업무별 기준선 적격 등급

| 등급 | 조건 | 처리 |
|---|---|---|
| 미완성 | `n < 8` 또는 SD 계산 불가 | 점수 미산출 |
| 임시 | `8 ≤ n < 20` | 점수는 계산하되 사용자 알림·주간 판단에는 미사용 |
| 적격 | `20 ≤ n < 30` | 일일·주간 점수 및 케어 기록에 사용 가능 |
| 안정 | `n ≥ 30` | 기준선 업데이트 후보로 사용 가능 |

업무마다 편차가 매우 큰 경우에는 표본 수가 충분해도 해당 업무의 시간 Z-score만으로 판단하지 않는다. 이 경우 정지시간·업무 순서·재방문 같은 다른 지표가 추가될 때까지 “참고용”으로 유지한다.

---

## 12. 다양한 촬영 각도·자세를 고려하는 지표

### 12.1. 가능한 범위

가능하다. 다만 서로 다른 카메라 각도나 몸의 방향에서 나온 **원본 2D 좌표를 직접 비교하면 안 된다.** 같은 “음료 제조”라도 정면·측면·등을 보인 상황에서 손의 x/y 궤적은 크게 달라진다.

초기에는 다음 두 층으로 분리한다.

1. **업무 자체의 변화**: 소요시간·정지시간처럼 각도 영향을 덜 받는 값
2. **시점 적합성**: 현재 동작이 기준선을 만든 카메라·몸 방향과 충분히 비슷한지

### 12.2. 시점 서명(View Signature)

각 동작 구간에 아래 값을 함께 저장한다.

$$
V = (cameraId,\ angleBin,\ visibilityBin)
$$

| 구성 요소 | 산출 방식 | 목적 |
|---|---|---|
| `cameraId` | 설치 카메라 고유 ID | 다른 카메라 좌표를 직접 섞지 않기 위함 |
| `angleBin` | 어깨 너비/몸통 길이 비율, 좌·우 어깨 depth 차이로 정면·사선·측면 구간화 | 몸 방향 차이 구분 |
| `visibilityBin` | 전신 주요 관절 가시성 평균을 높음·보통·낮음으로 구간화 | 가림·화면 잘림 구분 |

현재 코드에도 몸 비율(어깨·골반·다리 비율)과 관절 가시성은 있어 `angleBin`, `visibilityBin`의 시작값을 만들 수 있다. 다만 정확한 yaw 각도는 별도 보정 없이 추정값으로만 사용한다.

### 12.3. 시점 적합도 (View Compatibility Score, VCS)

현재 구간의 시점 서명과, 동일 업무 기준선 군집의 대표 시점 서명 간 거리를 `d_view`라고 한다.

$$
VCS = e^{-d_{view}}
$$

| VCS | 처리 |
|---:|---|
| 0.80 이상 | 같은 시점 기준선으로 Z-score 계산 가능 |
| 0.60~0.79 | 참고용으로만 계산, 사용자 알림에는 미사용 |
| 0.60 미만 | 시점이 달라 직접 비교하지 않고 새 시점 표본으로 축적 |

### 12.4. 시점 커버리지 (View Coverage Score)

동일 업무를 정면·사선·측면 등 `K`개 시점 구간에서 학습했을 때, 각 구간의 표본 비율을 `p_k`로 둔다.

$$
Coverage = -\frac{\sum_{k=1}^{K} p_k\ln(p_k)}{\ln(K)}
$$

- 범위: 0~1
- 목적: 이상 점수에 직접 더하는 값이 아니라, **특정 업무가 한 방향에서만 학습됐는지**를 확인하는 품질 지표
- 예: 카메라 1의 정면에서만 20회 학습했다면, 측면 동작은 새 기준선 표본으로 모으고 기존 정면 기준과 비교하지 않는다.

---

## 13. 다중 카메라 동일 대상 연결 지표

### 13.1. 현재 구현 상태

현재 프로토타입은 카메라 한 대의 세션 안에서만 대상을 고정한다. 한 화면 안에서는 몸 중심 이동, 크기 변화, 해부학적 비율의 연속성을 이용해 다른 사람이나 사물로 자동 전환하지 않도록 설계돼 있다. 하지만 카메라 1에서 카메라 2로 넘어간 대상을 동일인으로 연결하는 기능은 아직 없다.

### 13.2. 권장 원칙

얼굴·의상 이미지를 장기 저장하지 않는 현재 프라이버시 원칙을 유지한다면, “동일인 확정” 대신 **동일 대상 연결 신뢰도**를 계산해야 한다.

불확실할 때는 잘못 합치는 것(False Merge)보다 분리하는 것(False Split)을 선택한다. 연결이 애매한 카메라 경계 구간은 점수 계산에서 제외한다.

### 13.3. 카메라 간 연결 신뢰도 (CCAC)

카메라 1에서 종료된 tracklet A와 카메라 2에서 새로 시작된 tracklet B의 연결 점수다.

$$
CCAC = 0.45T + 0.35G + 0.20B
$$

| 구성 | 계산 예시 | 필요한 전제 |
|---|---|---|
| 시간 적합도 `T` | $e^{-\frac{(\Delta t-\mu_{route})^2}{2\sigma_{route}^2}}$ | 카메라 1→2 이동 시간의 개인/매장 기준 |
| 공간 적합도 `G` | $e^{-\frac{d_{ground}^2}{2\sigma_g^2}}$ | 카메라 시야 겹침 또는 바닥 평면 보정 |
| 몸 비율 적합도 `B` | $e^{-D_{shape}/\tau_b}$ | 어깨·골반·다리/몸통 비율의 차이 |

- `T`: 카메라 1 출구와 카메라 2 입구가 정해졌을 때, 예상 이동 시간 안에 이어졌는지를 본다.
- `G`: 카메라가 겹치는 구역이라면 보정된 바닥 좌표상 이동 궤적이 이어지는지를 본다.
- `B`: 현재 대상 고정에서 쓰는 몸 비율을 재식별 보조값으로 쓴다. 이 값만으로 신원을 확정하지 않는다.

### 13.4. 연결 판정 정책

| CCAC | 처리 |
|---:|---|
| 0.85 이상 | 동일 대상 tracklet으로 연결 |
| 0.65~0.84 | 연결 보류. 카메라 경계 전후 구간은 점수·알림에서 제외 |
| 0.65 미만 | 다른 tracklet으로 분리 |

카메라가 서로 겹치지 않는다면 `G`를 계산할 수 없으므로 `T`와 `B`의 비중을 재정규화한다. 이 경우에도 0.85 이상을 얻지 못하면 연결하지 않는다.

### 13.5. 다중 카메라 도입 전 필수 데이터

- 모든 카메라의 `cameraId`
- 공통 시간 기준(겹치는 카메라는 권장 오차 200ms 이내)
- 카메라별 관찰 Zone, 입구·출구 Zone, 카메라 간 허용 이동시간
- 카메라별 대상 tracklet ID와 시작·종료 시각
- 몸 비율 대표값·가시성 품질·시점 서명
- 연결 결과, `CCAC`, 연결/보류/분리 상태

### 13.6. 다중 카메라 품질 평가 지표

| 지표 | 정의 | 초기 목표 |
|---|---|---:|
| 연결 정밀도 | “같은 대상”으로 연결한 tracklet 중 실제 같은 대상 비율 | 95% 이상 |
| False Merge Rate | 다른 사람을 같은 대상으로 합친 비율 | 1% 이하 |
| ID Switches / 100 handoffs | 100회 카메라 이동 중 대상 ID가 바뀐 횟수 | 2회 이하 |
| 연결 보류율 | 애매해서 점수에서 제외한 handoff 비율 | 초기엔 측정 후 허용 범위 설정 |

> 다중 카메라 추적은 카메라별 추적, tracklet 생성, 시간·공간 제약, 카메라 간 연결을 결합하는 별도 문제다. 한 대의 카메라 안에서의 대상 고정과는 구현 난이도가 다르다.

---

## 14. 코드 적용 시 필요한 최소 변경 범위

이 문서를 실제 구현으로 옮길 때 필요한 범위다. **현재는 적용하지 않는다.**

1. `TaskBaseline`에 업무별 최소 표본 수 검증을 추가한다.
2. `durationZScore`, `pauseZScore`, `contextWeight`로 `ECE`를 계산하는 순수 함수를 추가한다.
3. 동일 업무·2시간 단위 중복 제거 후 `DCB`, `DFS`를 계산한다.
4. 현재 케어 기록의 기존 지표(마감·업무 누락)와는 우선 분리해 병렬 검증한다.
5. 사용자 피드백의 사유를 점수 억제 규칙에 연결한다.
6. 업무별 표본 수 적격 등급, 시점 서명, VCS를 추가해 서로 다른 각도에서의 잘못된 비교를 막는다.
7. 다중 카메라 도입 시에는 cameraId·tracklet·CCAC를 별도 파이프라인으로 구현한다.
8. 실제 매장 데이터에서 2~4주간 점수 분포와 오탐률을 확인한 뒤 사용자 UI에 반영한다.

---

## 15. 한 줄 결론

> 현재 프로토타입에서는 **업무별 소요시간·정지시간 Z-score와 맥락 가중치만으로 0~100의 “평소 흐름 일치도”를 만들 수 있다.** 단, 8회는 탐색용 최소선일 뿐이며 사용자 알림은 업무별 20회 이상 기준선부터 시작한다. 다양한 시점과 다중 카메라 연결은 별도의 적합도·연결 신뢰도 조건을 통과한 경우에만 점수에 반영한다.
